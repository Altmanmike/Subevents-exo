{% extends 'base.html.twig' %}

{% block title %}Home page{% endblock %}

{% block body %}

<div class="container mx-auto my-5 w-50">
    <h1 class="my-5">SubEvent-exo home page ✅</h1>

    Vous avez la possibilitéde voir l'api et/ou d'entrer dans le tableau de bord de subEvents :
    <ul class="my-4">        
        <li class="my-2">API subEvents JWT (à faire) : <a href="{{ path('app_app') }}" class="btn btn-sm btn-outline-secondary">ici</a></li>      
        <li class="my-2">Dashboard subEvents: <a href="{{ path('admin') }}" class="btn btn-sm btn-outline-primary">ici</a></li>
    </ul>

    {% if app.user %}
        <div class="mb-3">
            {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Se déconnecter</a>
        </div>
    {% endif %}

</div>
{% endblock %}
